# Giai đoạn 1 - Xử lý dữ liệu
pdfplumber>=0.11.0      # trích xuất text + bảng từ PDF
python-docx>=1.1.0      # đọc file Word (.docx)
olefile>=0.47           # đọc file Word 97-2003 (.doc, định dạng nhị phân OLE2)
openpyxl>=3.1.0         # đọc file Excel (.xlsx)
pandas>=2.2.0           # xử lý bảng Excel
tqdm>=4.66.0            # thanh tiến trình
easyocr>=1.7.0          # OCR tiếng Việt cho PDF scan (chạy GPU)
pymupdf>=1.24.0         # render trang PDF -> ảnh cho OCR
img2table[easyocr]>=1.3.0   # OCR BẢNG (giữ cấu trúc hàng/cột) cho trang scan

# Giai đoạn 2 - Embedding & Vector Database
sentence-transformers>=3.0.0   # nạp BGE-M3 và mô hình rerank
chromadb>=0.5.0                # vector database lưu bền
rank_bm25>=0.2.2               # tìm theo từ khóa (truy xuất lai)

# Giai đoạn 3 - RAG & LLM
transformers>=4.45.0           # nạp & chạy LLM (Qwen2.5-3B-Instruct)
accelerate>=0.34.0             # hỗ trợ nạp model lên GPU

# Giai đoạn 4 - Fine-tuning QLoRA
peft>=0.13.0                   # LoRA adapter
bitsandbytes>=0.44.0           # lượng tử hóa 4-bit (QLoRA)
trl>=0.11.0                    # SFTTrainer
datasets>=3.0.0                # nạp dataset huấn luyện

# Giai đoạn 5 - Giao diện web
gradio>=5.0.0                  # giao diện chat web
pyvi                # tách từ tiếng Việt — dùng để ĐỐI CHIẾU nội dung khi tinh chỉnh Q/A
